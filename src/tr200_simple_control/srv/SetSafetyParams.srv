# SetSafetyParams.srv
# 안전 파라미터 설정 서비스 정의

# 요청 메시지 (클라이언트가 보내는 데이터)
float32 min_obstacle_distance       # 최소 장애물 거리 (m)
float32 warning_distance            # 경고 거리 (m)
float32 safe_distance               # 안전 거리 (m)
float32 normal_speed                # 정상 속도 (m/s)
float32 slow_speed                  # 감속 속도 (m/s)

---

# 응답 메시지 (서버가 보내는 데이터)
bool success                        # 성공 여부
string message                      # 응답 메시지
